{% extends 'base.html.twig' %}

{% block title %}Edit Appeal{% endblock %}

{% block body %}
    <h1>Редактировать обращение</h1>

    <div class="col-sm-6 col-lg-3">
        {{ include('appeal/_form.html.twig', {'button_label': 'Редактировать'}) }}
    </div>

    <a href="{{ path('appeal_index') }}">back to list</a>
{% endblock %}
